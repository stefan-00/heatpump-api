## 1. Manifest cleanup

- [x] 1.1 Remove `- armv7` from the `arch` list in `heatpump-api/config.yaml`
- [x] 1.2 Bump `version` in `heatpump-api/config.yaml` to `0.1.4`
- [x] 1.3 Add a `0.1.4` entry to `heatpump-api/CHANGELOG.md` (drop deprecated armv7 arch)

## 2. Documentation

- [x] 2.1 No code change for the `openspec/config.yaml` scan warning — captured in the spec as expected/benign

## 3. Archive

- [x] 3.1 Verify and archive the change, syncing the `haos-addon-packaging` delta into `openspec/specs/`
